services:
  postgres:
    image: postgres:17-alpine
    container_name: hmo-postgres
    environment:
      POSTGRES_DB: hmo_central
      POSTGRES_USER: hmo
      POSTGRES_PASSWORD: hmo_secret
    ports:
      - "5432:5432"
    volumes:
      - hmo_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U hmo -d hmo_central"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  hmo_postgres_data:
